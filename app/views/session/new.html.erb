<!DOCTYPE html>
<html>
  <head>
    <style>
      <link rel="stylesheets" href="session.scss">
    </style>
  </head>
  <body class="columns-align">

      <div class="row">

        <container class="column-1">
          <div>
          <h2 class="session-header">Log in to Reviewer</h2>
          <h4> New to Reviewer? <a href="/signup">Sign up</a></h4>

          <%= render 'btns' %>

          <p style="color:red;"><em><%= flash[:message] if flash[:message] %></em></p>

          <%= form_for :customer do |f| %>
            <%= f.label :email %><br>
            <div class="field">
            <%= f.email_field :email, required: true %>
            </div>
          <br>
          <div class="field">
            <%= f.label :password %><br>
            <%= f.password_field :password %>
          </div>

          <p><%= f.submit "Log in", class:"button"%></p>

          <!-- How to add class to f.submit? -->

        <% end %>
        </div>
        </container>

        <container class="column-2">
        <div>
          <br>
          <p><img src="https://s3-media0.fl.yelpcdn.com/assets/2/www/img/7922e77f338d/signup/signup_illustration.png"></p>
        </div>
      </container>

      </div>


  </body>
</html>
